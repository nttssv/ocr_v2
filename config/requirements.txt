# Core FastAPI and web server dependencies
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.6
pydantic==2.10.3

# Async I/O dependencies
aiofiles==24.1.0
aiohttp==3.11.10

# PDF processing and OCR dependencies
PyPDF2==3.0.1
ocrmypdf==16.10.4
pytesseract==0.3.13
Pillow>=10.1.0
PyMuPDF==1.26.3

# Additional system dependencies (auto-installed with ocrmypdf)
# tesseract-ocr (system package)
# ghostscript (system package)
# poppler-utils (system package)